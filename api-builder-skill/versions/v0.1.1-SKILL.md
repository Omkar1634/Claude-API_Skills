---
name: api-builder
description: Use when the user wants to build, scaffold, or create a backend API or REST endpoint. Guides the developer through a structured conversational flow — understanding their project first, then collecting technical preferences one question at a time — before generating a complete, runnable project using real entity names.
---

# API Builder Skill

## Overview

API Builder is a conversational Claude Skill that helps developers scaffold a backend API project through a structured, question-driven flow. It never assumes your stack, never dumps multiple questions at once, and never generates code until it fully understands what you need.

---

## Trigger Phrases

Activate this skill when the user says any of the following (or close variations):

- "build an API"
- "create a backend"
- "scaffold my API"
- "help me build a REST API"
- "I want to create an API"
- "set up a backend project"

---

## Core Rules (Always Enforced)

1. **One question at a time** — never ask multiple questions in a single message.
2. **Never assume the framework** — always ask language first, then framework.
3. **Always ask permission before reading existing code** — do not inspect files without explicit user consent.
4. **Always show an implementation plan before touching any file** — no edits without a confirmed plan.
5. **Generated code uses real entity names** from user input — never generic placeholders like `MyModel` or `YourEntity`.
6. **Ask for confirmation of the full summary** before generating any code.

---

## Conversational Flow

### Phase 0 — Trigger

When the skill activates, greet the user and ask the first question:

> "Let's build your API! First — are you starting from scratch, or do you have an existing project you'd like to extend?"

---

### Phase 1 — Scratch or Existing Project?

**Branch A — Scratch:**
Proceed to Phase 2.

**Branch B — Existing project (has API code already):**
Proceed to Phase 1B.

**Branch C — Mid-project (partial context — e.g. has a frontend but empty backend, or has a database but no routes):**

Acknowledge what they have, then ask what they need:

> "Got it — so you've already got [what they described]. I'll build the backend API to connect with that. Before I start, tell me: what does the API need to do? What data or actions should it expose?"

Then proceed to Phase 2 using what they've described as context. Do NOT ask them for existing code since the backend is empty. Do NOT treat this as a full scratch project either — reference what they've already built when making suggestions (e.g. CORS config for their frontend, matching their existing data shape).

---

### Phase 1B — Existing Project Flow

1. **Ask permission to read the code:**
   > "Before I look at anything, do I have your permission to read your existing code? You can either share the files directly or paste the relevant code here."

2. Wait for user to share files or paste code. Do not read or reference any code until they do.

3. **After reading the code, show an implementation plan:**
   > "Here's what I'd suggest adding to your project: [plan summary]. Does this look right before I proceed?"

4. **Ask how they'd like to receive changes:**
   > "Would you prefer I edit the files directly, or would you like me to show you the code blocks to copy in yourself?"

5. Wait for user confirmation before making any changes.

---

### Phase 2 — Understand the Project

Ask these questions **one at a time**, waiting for a response before moving to the next:

1. "What are you building? Give me a brief description of the project."
2. "What are the main entities or resources in your project? (e.g., users, products, orders)"
3. "Who will be calling this API? (e.g., a web frontend, mobile app, internal service, third-party clients)"

---

### Phase 3 — Technical Preferences

Ask these questions **one at a time**:

1. "Which programming language would you like to use?"

2. "Which framework would you like to use?"

   After the user answers the language question, suggest relevant frameworks based on their choice:

   | Language | Suggest |
   |---|---|
   | Python | FastAPI, Django REST Framework, Flask |
   | JavaScript / Node.js | Express, Fastify, NestJS |
   | TypeScript | NestJS, Fastify, Express |
   | C# | ASP.NET Core Web API |
   | Java | Spring Boot |
   | Go | Gin, Echo, Fiber |
   | PHP | Laravel, Slim |
   | Ruby | Rails API, Sinatra |

   Present the top 2–3 options for their language and let them choose. Do not pick for them.

3. "Which database would you like to use?"

4. "How would you like to handle authentication?" *(e.g., JWT, API keys, OAuth2, session-based, none)*

5. "How would you describe your experience level with this stack?" *(beginner / intermediate / advanced)*

**Handling unsupported frameworks:**
If the user picks a language or framework outside the skill's current support scope, respond:

> "That framework isn't fully supported in this version of the skill, but I can still help you with a general structure and point you in the right direction. Would you like to proceed with that, or choose a different framework?"

**v1 Supported stack:**
- Language: Python
- Framework: FastAPI
- Database: SQLite
- Auth: JWT
- Generation method: Inline code blocks

---

### Phase 4 — Features

Before presenting the feature list, apply these rules to **pre-suggest relevant features** based on what was learned in Phase 2 and 3:

| If... | Then suggest... |
|---|---|
| API is public-facing (web/mobile frontend) | Rate limiting, CORS config, pagination |
| User is a beginner | OpenAPI / Swagger docs (so they can test easily) |
| Deploying locally | Skip Docker (don't suggest it unless asked) |
| Deploying to cloud / VPS | Docker support |
| Project has list-based resources (products, orders, posts) | Pagination |
| Multiple services or microservices mentioned | Structured logging |
| Auth is JWT | Token expiry note, no refresh tokens unless asked |

Present pre-suggested features clearly, then offer the full list for the user to add or remove:

> "Based on what you've described, I'd suggest including: [pre-suggested list]. Would you also like any of these: [remaining options]? Or anything else you have in mind?"

Full feature options:
- Rate limiting
- Caching
- Pagination
- Response compression
- Structured logging
- Unit / integration tests
- OpenAPI / Swagger documentation
- Docker support

---

### Phase 5 — Confirm Summary

Before generating anything, play back the full configuration to the user:

> "Here's everything I've captured before I generate your project:
>
> - **Project:** [description]
> - **Entities:** [list]
> - **API consumers:** [who calls it]
> - **Language:** [language]
> - **Framework:** [framework]
> - **Database:** [database]
> - **Auth:** [auth method]
> - **Experience level:** [level]
> - **Features:** [list]
>
> Does this look correct? Say 'yes' to generate, or let me know what to change."

Do not generate any code until the user confirms.

---

### Phase 6 — Generate

Generate the full project inline as code blocks. Use **real entity names** from the user's input throughout — in model definitions, route handlers, schemas, and database tables.

Generate files in this order, explaining each file briefly if the user is a beginner:

**Project folder structure (FastAPI + SQLite + JWT):**

```
my-api/
├── app/
│   ├── __init__.py
│   ├── main.py              — FastAPI app entry, mounts routers, registers middleware
│   ├── config.py            — Settings via pydantic-settings, reads .env
│   ├── dependencies.py      — Shared DI: db session, current user
│   ├── api/
│   │   ├── __init__.py
│   │   └── v1/
│   │       ├── __init__.py
│   │       ├── router.py    — Aggregates all v1 sub-routers
│   │       └── endpoints/   — One file per resource (e.g. auth.py, users.py, products.py)
│   ├── core/
│   │   ├── security.py      — JWT encode/decode, password hashing
│   │   ├── rate_limit.py    — slowapi integration
│   │   ├── exceptions.py    — Global HTTP exception handlers
│   │   ├── logging.py       — Structured logging setup
│   │   └── pagination.py    — Reusable page/limit query params
│   ├── db/
│   │   ├── session.py       — SQLAlchemy engine + SessionLocal
│   │   ├── base.py          — DeclarativeBase all models inherit
│   │   └── migrations/      — Alembic migration files
│   ├── models/              — SQLAlchemy ORM table definitions (one file per entity)
│   ├── schemas/             — Pydantic request/response models (one file per entity)
│   │   └── common.py        — PaginatedResponse, ErrorDetail
│   └── services/            — Business logic layer (one file per entity)
├── tests/
│   ├── conftest.py          — Test DB setup, fixtures, test client
│   ├── unit/                — Service layer tests
│   └── integration/         — Endpoint tests via httpx AsyncClient
├── .env.example
├── pyproject.toml
├── requirements.txt
├── requirements-dev.txt
├── alembic.ini
├── Dockerfile               — (if Docker selected)
├── docker-compose.yml       — (if Docker selected)
├── .gitignore
└── README.md
```

Each file must be:
- Complete and immediately runnable
- Named after real entities (e.g. `products.py` not `resource.py`)
- Free of `TODO` comments and placeholder values
- Explained in 1–2 sentences if the user is a beginner

---

### Phase 7 — Follow-Up Loop

After generation, offer these options:

> "Your project is ready. What would you like to do next?
>
> 1. Add a new endpoint
> 2. Generate tests
> 3. Show the OpenAPI spec
> 4. Add Docker support
> 5. Something else"

Handle each selection, then return to this menu. Continue until the user is done.

---

## Version

Current skill version: **v0.1.0**
